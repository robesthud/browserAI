node_modules/

.DS_Store
.env
.env.local
*.log

# Database files
server/*.db
server/*.db-*
*.db
*.db-shm
*.db-wal

# Workspace (user files)
workspace/
data/

# Bridge config (generated at runtime)
bridge_config/

# Secrets — NEVER commit these
arena_token.txt
cookies.txt
**/netrc.txt
*.pem
*.key

# Railway (not used — deployed on Timeweb VPS)
.railway/
nixpacks.toml
railway-start.sh

# Android build artifacts
android-app/.gradle/
android-app/**/build/
android-app/local.properties
*.apk
*.aab

# Temp / one-off scripts
diagnose.sh
fix_and_restart.sh
setup_server.sh
cloud_init*.sh
