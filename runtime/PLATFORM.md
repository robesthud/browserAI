# This project now uses Privileged Agent Runtime Platform

Name: Privileged Agent Runtime Platform
Also known as: Agent Runtime, Privileged Agent Runtime, Privileged Tool Execution Layer

All core tools (bash, git_commit, etc.) now execute directly with host privileges by default.
