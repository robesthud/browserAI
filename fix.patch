--- tests/sse-stream-shape.test.js
+++ tests/sse-stream-shape.test.js
@@ -37,13 +37,11 @@
 })
 
 // Correctly mock cost tracker and checkCap
-vi.mock('../server/costTracker.js', async (importOriginal) => {
-  const mod = await importOriginal()
-  return {
-    ...mod,
-    recordSpend: vi.fn(),
-    checkCap: vi.fn(() => ({ ok: true, reason: '' })),
-    chatTotalUsd: vi.fn(() => 0),
-  }
-})
+vi.mock('../server/costTracker.js', () => ({
+  recordSpend: vi.fn(),
+  checkCap: vi.fn(() => ({ ok: true, reason: '' })),
+  chatTotalUsd: vi.fn(() => 0),
+}))
 
